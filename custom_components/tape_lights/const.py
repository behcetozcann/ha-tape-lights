"""Constants for the TAPE LIGHTS integration."""

DOMAIN = "tape_lights"
DEFAULT_NAME = "TAPE LIGHTS"

DEFAULT_SPEED = 50
DEFAULT_MIC_SENSITIVITY = 50

# Drop the BLE link after this many idle seconds so the phone app can connect.
IDLE_DISCONNECT_SECONDS = 30
